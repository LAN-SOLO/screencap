{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "screencap windows (main + region overlay)",
  "windows": ["main", "region"],
  "permissions": [
    "core:default",
    "dialog:default",
    "opener:default"
  ]
}
